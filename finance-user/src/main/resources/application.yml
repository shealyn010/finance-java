server:
  port: 8081

spring:
  application:
    name: finance-user
  datasource:
    url: jdbc:mysql://localhost:3306/fininsight?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  security:
    user:
      name: admin
      password: admin123

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
